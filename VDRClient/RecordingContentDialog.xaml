<ContentDialog
    x:Class="VDRClient.RecordingContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VDRClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="RecordingContentDialog"
    Title="Action"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <RadioButton Grid.Row="0" x:Name="watchRadioButton" Content="Watch" Tag="watch" Checked="RadioButton_Checked" />
        <RadioButton Grid.Row="1" x:Name="deleteRadioButton" Content="Delete" Tag="del" Checked="RadioButton_Checked" />
        <RadioButton Grid.Row="2" x:Name="removeRadioButton" Content="Remove" Tag="remove" Checked="RadioButton_Checked" />
    </Grid>
</ContentDialog>
